<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textViewStyle">@style/TextStyle.Generic</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="TextStyle.Generic" parent="android:Widget.TextView">
        <!--<item name="android:fontFamily">serif</item>-->
        <item name="android:textColor">@color/gray90</item>
    </style>

    <style name="TextStyle.DialKeys" parent="TextStyle.Generic">
        <item name="android:textSize">@dimen/dial_numbers</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonStyle" parent="android:style/Widget.Button">
        <item name="android:backgroundTint">@color/gray_40</item>
    </style>
</resources>